package com.customer360.backend.config;

public class DataSeeder {
    // Disabled for now.
    // Reason: login is currently using in-memory demo users.
    // This avoids MongoDB startup dependency while Atlas connection is unstable.
}
